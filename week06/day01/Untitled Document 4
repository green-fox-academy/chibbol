#include <stdio.h>
#include <stdint.h>

void printf_numbers(long int from, long int to)
{
	for (long int i = from; i < to; i++)
		printf("%ld,", i);
}

int main()
{
	printf_numbers(2147483600, 2147484600);
	return 0;
}

